<form method="post" action="{{ path('app_lunette_delete', {'id': lunette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lunette.id) }}">
    <button class="btn">Delete</button>
</form>
