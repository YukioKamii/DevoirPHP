{% extends 'base.html.twig' %}

{% block title %}Lunette{% endblock %}

{% block body %}
    <h1>Lunette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lunette.id }}</td>
            </tr>
            <tr>
                <th>Serie</th>
                <td>{{ lunette.serie }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ lunette.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lunette_index') }}">back to list</a>

    <a href="{{ path('app_lunette_edit', {'id': lunette.id}) }}">edit</a>

    {{ include('lunette/_delete_form.html.twig') }}
{% endblock %}
